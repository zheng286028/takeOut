server:
  port: 9002
spring:
  shardingsphere:
    datasource:
      names:
        master,slave
      # 主数据源
      master:
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://192.168.213.128:3306/takeout?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8
        username: root
        password: root
      # 从数据源
      slave:
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://192.168.213.138:3306/takeout?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8
        username: root
        password: root
    masterslave:
      # 读写分离配置
      load-balance-algorithm-type: round_robin #轮询，从库一个接�?�?个的�?
      # �?终的数据源名�?
      name: dataSource
      # 主库数据源名�?
      master-data-source-name: master
      # 从库数据源名称列表，多个逗号分隔
      slave-data-source-names: slave
    props:
      sql:
        show: true #�?启SQL显示，默认false
  main:
    allow-bean-definition-overriding: true
  #redis数据�?
  redis:
    host: 192.168.213.128
    port: 6379
    database: 0
    #缓存有效时间
  cache:
    redis:
      time-to-live: 1800000
  mvc:
    hiddenmethod:
      filter:
        enabled: true
#邮箱
  mail:
    host: smtp.qq.com
    username: 2860285053@qq.com
    password: ajqmtiykwechdcij
#MP配置
mybatis-plus:
  configuration:
    #在映射实体或者属性时，将数据库中表名和字段名中的下划线去掉，按照驼峰命名法映�?
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: ASSIGN_ID
#图片地址
reggie:
  path: C:/img/




